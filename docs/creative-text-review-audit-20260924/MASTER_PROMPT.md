# 交给执行 Coding Agent 的完整任务

你接管 `/Users/jinkun.wang/work_space/veidofactory`，实现已经由用户确认、经 Oracle 审计无实质产品异议的“所有面向创作者的文字交付：初稿独立审一次，后续由用户决定”的交互。**这是一项完整实施任务，不是只修创作规划三阶段或只写方案。**

## 第一步：完整阅读并建立基线

按顺序完整阅读本目录的 `ORACLE_BRIEF.md`、`ORACLE_VERDICT.md`、`IMPLEMENTATION_CONTRACT.md`、`ACCEPTANCE.md`。Oracle 原始完整答复可选读：`/Users/jinkun.wang/.local/state/oracle-web/sessions/vf-text-creation-review-20260924-06/artifacts/transcript.md`。以 `IMPLEMENTATION_CONTRACT.md` 的用户规则和 `ACCEPTANCE.md` 的可证伪验收为准，不把 Oracle 的未附源码推测当成线上已证事实。

先执行 `git status --short`、`git rev-parse HEAD`，保留当前所有已有更改与未跟踪文档；不要 reset、checkout、清空工作树、修改密钥或历史生产数据。查仓库内实际 `AGENTS.md`、`package.json` 与 CI 命令。不要 commit、push、建 PR 或部署。不要把本任务扩展成模型配置、素材库、UI 全站重设计或媒体 Provider 重构。

在本目录建立 `RESULT.md`，先写 S0 发现表：十二种 Broker task 的真实调用点、产物去向、用户入口/停点、是否支持审计/修订/历史、默认循环次数、checkpoint/恢复方式；特别核实 `reference-grammar`、`series-roadmap` 绿灯、`publish-copy write()/writeDetailed()`、`topic-ideas` 选择入口，以及返工范围冲突进入 `failed` 的实际调用链。每项标明源码路径与行号。若发现与合同的“事实背景”不同，更正事实；不要借此私改用户决定的行为。

## 实施顺序和退出条件

按 `IMPLEMENTATION_CONTRACT.md` 的 S1→S6 执行：

1. **先失败测试，后改代码。**先让 A/B/C/D 关键反例在现有实现中失败，记录失败摘要；实现对应行为后让聚焦测试通过。不要把旧测试直接删掉或改成弱断言以制造绿色。
2. **S1 版本/动作/回执**：明确 `讨论/解释`、`提出备选`、`发送修订`、`主动审计`、`采用当前稿`；支持诚实的未审采用；稿/审/决定以不可变版本和操作身份绑定；stale、幂等、重启恢复与 A→B→A 反例一起做。只抽取实际多节点共享的最小合同，不另建完整工作流框架。
3. **S2 返工范围**：零媒体不能推导零影响；在非法候选成为有效稿前阻止其覆盖，提供显式可恢复的人工作范围决定。保留 worker 最终防线；扩范围不等于新费用授权。已失败 run 不洗白历史。
4. **S3 三个规划阶段**：treatment/script/director 初稿各自产一次审一次，展示后停人；修订只产稿；主动审计只审稿；采用当前稿不补审当前节点。导演 `direction` 和 `material_plan` 决定身份不能合并。
5. **S4 其他内容交付**：完整覆盖 `topic-ideas`、`series-roadmap`、`publish-copy`，以及当前可见可编辑的 `reference-grammar`；内部评价任务不递归审。修正所有生产消费旁路，不是给测试专门接一条假路径。
6. **S5 页面**：稿件和内容建议同屏；审计建议可多选并追加至已有文字，不覆盖、不自动发送；所有有效建议包括 `pass` 下的建议可见；技术字段留机器层，用户看到作品内容相关意见；主动审计/发送修订/采用分开；历史离开停点仍能查看。
7. **S6 验证**：逐项完成 `ACCEPTANCE.md`，聚焦、跨进程、全仓测试、typecheck、build、diff-check 都记录真实退出码。启动真实本地 Studio/服务端和实际持久化做受控浏览器 QA；不得用假页面、改 run.json 或绕过按钮伪造证据。未经本轮明确费用授权，不调用真实付费模型/媒体 Provider，也不宣称真实模型或成片端到端通过。

每个阶段修完就跑该阶段聚焦测试并更新 RESULT，但**不要停在 S3 就宣布“所有文字节点完成”**。如需要扩展合同，请先证实真实消费者；选择能满足当前用户行为的最小实现。若遇到必须新增的产品决策（而非普通工程取舍），在 RESULT 中列出证据、选项和推荐，暂停那一项；其他独立项继续做。

## 不可违背的行为红线

- 用户明确要求和已确认上游优先于系统建议；质量建议只能建议，不能自动重写、自动推进或阻断普通质量取舍。
- 不得移除事实、来源、结构、素材许可、预算/报价、未知已受理请求、返工范围硬校验。质量接受不是费用授权。
- 审计一轮必须是独立调用、绑定冻结稿版本；一次审计不等于产审修三轮。修订后不自动再审；未经审计的修订不能显示通过。审核失败不把整条生产误判永久失败，但未知已受理操作仍走原请求恢复。
- 明确 `discuss` 不授权改稿；模型返回越权 `intent` 不能直接改变有效稿。`propose` 不等于采用。
- 不用 hash 代替不可变版本；不把旧稿/旧建议/旧风险确认作用于新稿。已确认历史可回看，但不能篡改。
- 不给 `role-audit`、素材评分和视听审查再加递归审计；不让讨论中的每条消息产生新人工停点。
- 不改全局 `runRoleAgentLoop` 默认轮数来掩盖节点差异；保留原恢复/checkpoint/费用保护。

## RESULT.md 必须交付的内容

1. S0 真实调用矩阵，哪些原先的判断被证实/推翻，最终十二 task 归类。
2. A01–D05 每项的 `PASS / FAIL / BLOCKED / NOT_VERIFIED`、证据文件或测试断言；不要用一句“全绿”替代。
3. 修改文件及每个文件的必要性，关键合同/版本变更与兼容方式。
4. 先失败测试证据、聚焦测试及 `npm test` 等实际命令、退出码和跳过项；输出截断或超时不算通过。
5. 本地浏览器 QA 的 runId、截图路径、操作与持久化/重启证据；每类模型调用次数和费用账本。若没有真实外部调用，明确写“外部模型/成片未验证”，不能写真实 E2E 通过。
6. 剩余风险和需要用户决定的事项。只有全部目标行为、相关全量回归和真实本地受控 QA 有证据，才标 `READY_FOR_REVIEW`；否则如实标 `DONE_WITH_CONCERNS` 或 `BLOCKED` 并说明具体原因。

完成实现和验证后停止，交由审查方统一复核；不要自行推送或部署。
