# R3 执行后复审：先收口创作与恢复闭环

日期：2026-09-12。审查窗口：Codex。裁定：**CHANGES_REQUIRED**。

本文件审查 `RESULT.md` revision 1 和 QA04；不是重新实施 A/B/C，也不是整个产品的生产就绪声明。协调入口仍只有 `TASK.md` / `RESULT.md`，本文件是静态审查依据。本轮没有修改产品、调用真实模型或媒体、重启服务、提交或部署。

## 1. 结论与范围

执行端的底层修复有效：我独立运行的原始恢复探针与正式跨进程测试 **29/29 通过，exit 0**。不能把这一轮概括为“什么都没修好”。但从运行中的任务到用户可操作的恢复，以及创作方案到可执行校验，仍存在明确缺口。本轮三个新增探针 **3/3 失败，exit 1**。

有局部架构边界问题，但没有证据支持推翻工作流、换框架、加数据库/缓存/队列平台。当前需要收口的是：

1. **查询事实与用户动作一致**：查询一次任务状态，不应被实现成再等待一次完整模型结果；恢复必须认当前计划的实际任务，不能按文件修改时间猜。
2. **自然语言创作与机器校验分工正确**：结构化执行关系由代码严格校验；整段叙事语义由已有独立审计判断，不能用关键词误杀否定句。
3. **修订能收敛，耗时可解释**：同一问题不要通过重试、换 checkpoint 或跨角色返回重新获得隐形循环；不能修一个条件又破坏已合格的条件。

目标不变：保质量、提高高质量视频工作流的成功率、低门槛、可理解。不得以降模型/思考强度、删质量要求、静态卡片兜底或人工改状态放行来提速。也不能保证“修完必出爆款”。

## 2. 真实耗时与终态：更正报告中的推测

### 系列制作：16 分 39 秒主要花在模型执行，不是断网等待

运行 `run-41860eb5-9898-41f7-986e-88c2236a33a9`：12:39:04.471Z 至 12:55:43.495Z，999024 ms。

- 构思一轮通过，编剧三轮修订后耗尽，共八次生成/审计调用。
- 八次 trace 的 `providerWaitMs` 合计 **938161 ms（15 分 38 秒）**；`queueWaitMs` 合计 **56803 ms（57 秒）**；其余约 4060 ms。
- `phaseDurationsMs` 已包含对应等待，不能再把 queue 加一次。报告中不同统计范围的 produce/audit 数字不可混算。
- 可见推理 token 共 40633，输入约 16k–20k tokens。高强度模型真实执行占主要部分；这不等于已经证明模型本身异常，也不代表可以接受无效循环。
- 三轮反馈呈现反复：修开头兑现/条件后出现信息过载、素材能力冲突，再修这些又破坏开头。应修能力合同与修订约束的一致性，不能只延长超时。

### 概念制作：GLM 编剧已经通过，最终是导演校验失败

运行 `run-97298ba0-f2ff-4c38-a201-67c447235429`：12:48:10.580Z 至 13:10:55.034Z，**22 分 44 秒，最终 failed**。

- 构思通过；GLM 编剧两轮从 85 到 90 分，已通过。
- 导演两次生成均未过本地校验，尚未进入导演独立审计；两次 produce 合计 **469438 ms（7 分 49 秒）**。
- 最终保存候选中，触发人物连续性正则的唯一子句是：`独立生成仅承诺母题一致而非精确同一人物`。代码将否定关系当成肯定要求。这是本次失败的已证实原因，不应继续归咎于“GLM 卡住”或“模型输出不稳定”。
- 旧恢复 receipt 仍停留在 13:06:57Z 的 `accepted_unknown`。报告可能在后台尚未结束时采样；本次纠正的是后续终态，不指称当时虚报。以后收口必须刷新 run、当前 checkpoint 和 durable 事实。
- 已有六次可见 trace 不含两次无效导演调用的完整 provider/queue 分解，不把局部相加冒称覆盖全程。排除正则误判，也不等于该候选的整体视觉论证自动合格。

原始导航均在 `workspace/factory/runs/<runId>/`：`run.json` 与 `nodes/creative-planning/agent-loop-checkpoints/`。系列编剧 checkpoint 前缀 `e111fb35`；概念编剧 `dc936fc7`、导演 `fdc01856`。不要修改这些历史运行记录来“补测”。

QA04 的旧 65 分钟案例本轮没有独立重算完整 trace，仅作为历史背景；不能用它证明新版本仍原样循环。

## 3. 第一组：查询、恢复与当前计划绑定（P1，必须修）

### R3-01：运行中查询按钮消失——已复现

- 位置：`apps/studio/src/client/components/RunWorkbench.tsx:67`、`:374`。
- `taskRecovery` 被放在 `showReviewSurface` 内，普通 running 且无成片/干预时整个区域不渲染。API 已允许查询，用户却看不到入口。
- 原 `task-recovery-ui.test.tsx` 主要用 failed run，漏掉运行中的实际场景。
- 最小修改：恢复区域按服务端 `taskRecovery` 独立呈现，不依赖成片/失败面板，也不展示服务端未允许的动作；保持原只读、暂停与 mutation pending 约束。

### R3-02：健康运行误报“连接中断”——已复现

- 位置：`packages/production-pipeline/src/codex-chat.ts:410`、`apps/studio/src/server/production-studio.ts:1954`。
- 查询按钮调用 `observePrepared` 等三秒到终态；原任务只是仍在执行，也会到 deadline 后变成 uncertain，并显示连接中断。
- 最小修改：在已有 client 内提供带完整 immutable binding 的**单次查询结果**，供 Studio 使用。保留后台 worker 所需的等待到结果能力；不得将其整体替换成只查一次。
- DTO 区分最近已核验的任务事实与本次查询故障、观察时间。Broker 确证 running 就显示仍执行；只有存在 queued 证据才显示排队；观察期限到达不等于断网或执行失败。
- 查询不新增 POST、generation、审计轮数或 backup。若最近一次查询故障，保留此前事实并说明它的时间，不能把历史 running 冒充当前实时证明。

### R3-03：当前恢复对象靠 mtime 选择——源码确认，完整污染场景尚未复现

- 位置：`production-studio.ts:2379` 的 `loadPendingTextTask` 扫描所有节点 checkpoint，选择修改时间最新的 pending；`:2472` 的进度同样按 mtime 选择，且丢弃角色身份。
- 风险：旧版本/旧阶段的 pending 可被当成当前任务。receipt 写入当前 revision，并不能倒推出该 pending 原本属于当前计划。必须补正式 service 级失败用例后裁定具体影响，不声称已发生跨版本结果污染。
- `withTaskRecovery` 对非 paused 的 completed 给 `retrieve_and_continue`，但 `retrieveOriginalTextTaskOnce` 最终委托普通失败重试；后者拒绝 running。动作投影和执行前置条件也需在同一组修复中对齐。
- 最小修改：由现有 planning/run 状态确认当前 node、role、phase、有效计划/checkpoint key 和 operation binding，再查询或取回；复用现有 plan/store/lease/CAS，不建第二套恢复注册中心。旧文件无法证明归属就不可消费，不迁移/删除用户记录来解除门禁。
- 若 worker 仍持有执行权且能自行消费，只提供查询，不能让“取回”并发再推进。确需恢复时在真正消费前再次验证版本、binding 与执行权。暂停不得被查询解除。

本组验收 `R01`：running/paused/failed × running/unknown/completed/query-failure 的 API/组件矩阵；正常 running 查询无“连接中断”；暂停前后完成、编辑后旧结果晚到、旧 checkpoint mtime 更新、两个取回并发、Studio 重建均有正式测试。断言原请求执行次数、实际推进次数与动作，而不只匹配按钮文字。关联原 G03/G05。

## 4. 第二组：去除语义误杀，保留真实能力约束（P1，必须修）

### R3-04：否定句被人物关键词规则硬拒绝——已复现且对应真实失败

- 位置：`packages/production-pipeline/src/visual-evidence-boundary.ts:38`；`visual-director.ts:504`。
- `而非精确同一人物`、`并非同一人物`、`无需同一人物` 均被拒绝；现有否定白名单无法可靠理解自然语言。
- 不接受继续添加几组否定词、删除测试或对这个题材放行。

推荐实施路径：

1. 保留机器能证明的硬校验：交付类型、Provider 能力、`reuseFromScenePosition` / `referenceFromScenePosition`、引用方向和引用源、证据用途等。
2. 不再把自由文本中出现人物关键词作为 parser 拒绝方案的充分依据。将“叙事是否真的依赖同一主体、否定声明是否与实际论证相冲突”明确交给**已有导演独立审计**，确保它拿到脚本、完整候选、真实 Provider 能力和执行引用关系。不得多加一轮审计模型调用。
3. 真正要求身份连续但独立生成无支持的方案，独立审计必须给阻断与可执行修订，不能进批准方案/报价后付费执行。现有 task-definitions 已有相关规则，先查实际审计输入和 criteria 是否接通；不复制另一套提示词规则库。
4. 否定句不被 parser 误杀，不代表方案自动通过；“文字说不保证、因果论证却依赖同一对象”仍须审计拒绝。参考图只是可执行路线，不保证实际生成质量，成片审片仍要验。

本组验收 `R02`：通用否定、肯定、混合要求和文案/叙事矛盾案例；独立生成、同母片复用、合法参考图、无效引用的正式路径测试。自然语言负例可进入独立审计；不支持的身份要求被审计打回后不得报价授权/执行媒体。探针中的肯定句断言迁到实际方案验收边界，不要求永久保留当前 helper API，但必须保留“不支持的身份要求不能最终放行”的保障。静态 prompt 断言只能证明输入已传递，不能冒称真实模型判断已验证。

### R3-05：多轮修订互相破坏——真实 trace 已观察，提速效果仍待验证

- 不重写整个创作提示体系。核对构思、编剧、导演实际收到的可用素材/本地编辑能力与上游约束；只补确实漏传或矛盾的地方，复用正式目录。
- 现有 role loop 已传前候选和审计意见，不能宣称没有反馈机制。需要让修订明确保留原观众承诺、已合格要点，只改必要部分，并由已有审计检查是否回退。
- 对当前能力确实无法兑现的方案，尽早在现有干预路径说明缺什么、已有成果和用户选项；不等导演/图库多次兜圈，也不把承诺降为空泛情绪来通过。
- 只消除无效模型调用与错误返工；保留实际模型和强度，不设置没有依据的“文本必须 N 秒完成”验收。

本组验收 `R03`：正式角色适配器请求捕获能证明同一能力约束贯穿生成与审计；修订保留原承诺和已通过条件；用真实小样本记录生成/审计次数和终态，失败也如实展示。不能凭单条视频偶然更快宣称普遍提速。

## 5. 第三组：修订止损与耗时表达（收敛修复，不引入新平台）

### R3-06：普通重试获得新一轮预算——行为已确认，产品次数尚待确认

- `production-pipeline.ts:10062` 的 `nodeAgentLoopCheckpoint` 总是 `restartExhausted: true`；`role-agent-loop.ts:632` 为 exhausted 创建 `cycle + 1`，重置本轮计数。
- 内存 checkpoint 诊断表明：同输入连续运行两次，每次三轮，可产生六次 produce 和六次 audit。它证明现有“每循环三轮”不等于“本方案角色总共三轮”，不证明所有真实任务都会自动这样重试。
- 普通人工重试重开 cycle 是当前代码明确行为，不能不经产品确认就改成“一条视频一生只能三次”。RESULT 中关于用户总次数决定的转述，当前审查未拿到独立确认。
- 可直接保持/补齐的安全合同：查询、取回、进程恢复不重置次数；不改方案的自动重入不能伪装新创作轮次；明确改方案才形成新的创作身份。
- 推荐待确认语义：**同一方案、同一角色的自动修订有界；耗尽后显示最后阻断与已保留成果，用户调整方案后可以继续。** 创建/修订加一次独立审计算一轮，不把“审计”本身误记为“修改机会”。
- 全局总次数、人工额外追加次数、改变哪些字段算新一轮，不能由执行端自行硬编码。不要为了这一项决定阻止前两组确定性问题修复。

### R3-07：图库无进展判据过粗——静态确认

- `creative-planning.ts:432` 的 `availabilityBlockerDigest` 用固定中文 reason 识别，然后所有镜头问题共享一个固定 hash。
- 这能止损，但不是充分的无进展判断：解决了一个真实目标、出现另一个目标问题也可能算重复；措辞变化又可能漏识别。
- 最小修复：在现有 issue/checkpoint 内使用结构化原因和稳定叙事目标/受影响范围，比对实际可用证据是否改善。不能单用 query、文件时间、runId、artifactId 或镜头编号判断进展，也不需要新“智能收敛框架”。保留现有有界停止；补两种反例再改。

### R3-08：进度与耗时口径误导——源码确认

- 编剧 2/3 切到导演 1/3 可能是正常角色切换。当前进度丢失角色身份，所以显示像倒退；尚未证明实际计数被倒改。
- 当前 client 等待约 41 分钟，Broker 单任务约 20 分钟；不能只提高这两个值解决多轮创作耗时。
- `codex-executor.ts:845` 的首输出计时是首次 CLI stdout 事件，可能只是 thread.started；`zai-code-plan-executor.ts:296` 的首输出等于非流式完整响应耗时。二者不能当可比的首 token 延迟。
- 多个角色配置 `sessionMode: "stateless"`，因此空 sessions 是配置结果，不是已通过的 session 恢复测试再次失效。不盲目打开 stateful 来承诺提速。
- 最小修改：沿用已有 trace，显示角色、生成/审计/修订阶段与该角色轮数；汇总不重叠的排队、Provider 执行及本地处理时间。没有首内容证据就不称“首 token”。不新建监控平台，不把文案优化当真实性能修复。

本组验收 `R04`：同输入恢复与主动新规划分别测；查询次数不消耗修订轮数；同阻断换措辞不无限循环、不同目标确有改善不提前误停；角色切换和时长口径有测试。具体全局次数策略确认后才新增对应断言。

## 6. 集中执行与最终验收

下一轮先做 R01/R02，再将 R03/R04 已确认范围一并收口；同一轮完成后集中回归、真实 QA、统一审查。**不是每修一个问题就额外外审，也不是在单个样例上反复优化。** 待确认的总次数策略单列，不冒称实现。

最小代码范围：现有 `codex-chat`、Studio production service/DTO/RunWorkbench 与相关测试；`visual-director`、`visual-evidence-boundary`、已有角色审计接线；必要的 `creative-planning`/role checkpoint/进度投影。涉及正式模型输入合同的修改必须同步 Broker descriptor/parser/正式生产者测试，不能再产生两端 schema 不一致。

不做：新增创作业务、重选模型/框架/数据库、缓存平台、全库拆文件、Graphify 修复、另建 Agent 平台、云端部署。保留既有 dirty worktree，不把 A/B/C 历史改动算本轮成果。

集中验证：

1. 先迁入并补齐 R01–R04 的行为测试，再运行相关 Broker/role/service/UI 套件。需要故障注入的极端情况在隔离夹具验证，不用真实付费故障来碰运气。
2. 最后按 `ACCEPTANCE.md` G07 跑一次正式全量并保存完整退出状态。项目现有 Python unittest 131 项已通过；我之前要求必须 pytest 过重，现已纠正。没有 pytest 本身不是产品故障或付费阻断。`test:ts` 未保存退出状态仍须如实记录，下一次集中运行一并补齐，不为这个标记反复重跑所有套件。
3. 确认恢复/付费安全门后才申请或使用当前有效的小额真实 QA 权限。本审查本身不启动付费测试，也不自动唤醒执行窗口。
4. 至少一条真实制作走通方案、报价、媒体、配音、成片、两个模型独立审片和必要返工；其它入口尽量停报价避免重复购买。需要真实质量不通过时如实打回，不人为放行。
5. `E01–E07` 目前未全部通过：部分入口、暂停和响应式已有证据，但仍无本轮真实成片/双审/返工证据。不能因 29 项底层测试绿就宣布产品完成。

完成判断：已复现问题的正式回归通过；高风险恢复场景补到实际 service 层；用户在长等待时知道正在做什么、能查询/暂停/调整；最终成片与审片/返工有真实证据。若仍失败，记录同一运行的最新终态和根因，不另起无关优化项目。

## 7. 本次独立检查与证据边界

```sh
node --test --import tsx docs/codex-collaboration/evidence/transport-audit.test.mjs apps/codex-broker/test/task-lifecycle-v3.cross.test.ts apps/codex-broker/test/durable-owner-lifecycle.cross.test.ts
# 29 pass / 0 fail / 0 skipped / 0 cancelled，exit 0

node --test --import tsx docs/codex-collaboration/evidence/r3-review-probes.mjs
# 0 pass / 3 fail / 0 skipped / 0 cancelled，exit 1（待修问题的失败证据）
```

日志：`evidence/r3-review-existing-green.txt`、`evidence/r3-review-red.txt`。探针仅用临时 Broker 和依赖注入 executor、真实 client/UI 源码；未接正式 Studio、未调用模型或媒体。

本轮未独立重跑全量、浏览器和付费 E2E，不把执行者全量报告改称我的独立验证。R3-03 的跨版本污染、R3-07 的产品级误停尚待补正式复现；已指出的静态缺口与已发生的运行错误分开记录。

审查基线 HEAD `570fe6e59e072c4965c86769bf2096825f003383`，分支 `codex/final-dual-review-cloud-acceptance`。相对原 `BASELINE.json`，执行轮修改 46 个被跟踪基线文件，新增 7 个产品/测试文件；本审查不归属这些产品改动。下一次执行开始先记录新的文件级基线，原 BASELINE 保留，不覆盖历史证据。
