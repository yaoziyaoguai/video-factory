# 真实本地测试包（修复与回归后立即执行）

本包覆盖全链，不是只测新增六个修复。先重新完整读取当前安装的QA及浏览器技能；可用 `/Users/jinkun.wang/.agents/skills/gstack/qa/SKILL.md` 或当前安装的对应版本。按真实点击、表单、页面反馈、截图、console、移动布局、持久化进行；本任务不允许技能默认commit/clean-tree、逐bug重部署、遥测外发/升级。诊断可读源码/日志，但API脚本不能冒充点击证据。

## 一、恢复真实本地环境

审计时环境导航（启动前核对，PID不是授权杀进程清单）：

- URL `http://127.0.0.1:4319`，Studio86968/OpenAI86965/ZAI86967。
- 同一workspace：`/Users/jinkun.wang/work_space/veidofactory/workspace/qa-r11-repair-20260914`。
- runtime：`/Users/jinkun.wang/work_space/veidofactory/.local/runtime/qa-r11-repair-20260914`，下有 `start.mjs`、openai/zai/studio目录与日志。
- 两socket：runtime下 `{openai,zai}/worker.sock`。正式入口Broker `node apps/codex-broker/dist/main.js`，Studio `npm run studio:start`。
- 原实际模型sol/xhigh、GLM-5.3/max；ZAI有图路由glm-5.3-flash，以真实receipt为准，不仅看配置名。保留模型/强度/输出容量和deadline。

检查本runtime现有启动脚本，只复用其真实配置接线，不回显密钥/登录token，不复制secret到报告。执行前核对命令/cwd/监听端口、health与durable的在途；空闲且属于本轮才正常停止并用最终dist重启。不要宽泛pkill，不动旧4317或其它窗口服务。若发现新在途先观察原请求，不为部署中断它。

同workspace保留旧主片/系列/负例进行恢复验证，不另建空目录假装持久化成功。只读原证据；正常UI/API允许修本轮QA run。旧非R11/不明任务保持原样，不迁移数据。需要新样本仅用于未覆盖的明确用例，不能换题刷通。

确认：登录、所有实际所需Provider、11类Broker合同（如实现未新增kind）、parser/descriptor/digest、schema投影、最终build SHA均一致；能力健康不等于模型实际成功。用首次真实角色任务兼做就绪，不额外hello探测。禁止fake broker、伪造媒体/审片、测试库替身接正式环境。

## 二、预算：共用本轮50元，不重新开始计数

上一轮R11已知现金0、未知现金0、媒体/TTS未启动。启动前从实时quote/receipt/授权核对本轮全部run，不相信旧报告余额；R9历史8.75元单列，不混作R11新消费。

每次新增购买前：`已确定现金＋在途/未知/未结授权最大暴露＋本次新增最大暴露 ≤ 50元`。同scope父子去重，已付与未付余额不重复加。没有可确认上界就停止购买；unknown不当0。

授权范围合理且预算内由执行者自己点击确认/追加，无需用户逐笔审。主片优先留出TTS和一次局部返工余量，35元只是参考分配；不降质量/改目标凑预算。TTS自动执行仍记录实际费用，不额外创造TTS报价关卡。文本走订阅时不虚构现金价格。

旧GLM request `agent-e9fc146b34379bc729eb6f0cfcce4ff30586ce59be714157986fc13d6287ed60` 在 `.local/runtime/qa-r11-20260914-001/zai/...` 原记录accepted无outcome；只观察原ID，不清除/复制/换模型重投。订阅无新增现金不代表执行已失败。

## 三、测试顺序与固定样本

串行主模型流程，等待期间测无模型UI；不要同时刷新热点、启动系列、讨论主片填满单并发队列后误报“模型慢”。并发安全使用确定性测试，真实不制造付费断网。

### Q01 主片恢复与三阶段闭环（第一优先）

原主片 `run-25b4c195-a50c-4344-98fd-197d1bf58438`。目标20–35秒竖屏、忙碌城市上班族、匆忙到看见日常变化、少量自然中文旁白、开头吸引和结尾情绪兑现、可AI示意、非新闻/实验、无模板/说明卡。

1. 记录当前treatment/script稿SHA及确认。修复构成条件变化后，只从正式页面恢复失败director一次；不重做已确认上游、不改scene编号、不购买。
2. 记录初稿/内部修正实际调用与原因，生成合法分镜后必须正常停在讨论。看卡片/预计时长/获取路线/连续性/未生成说明是否易懂。
3. 一次“解释某镜头/复用安排”，方案和确认不暗改；确有必要时一次局部修订或备选采用，检查受影响范围、旧候选失效、无关稿保留。已做导演/脚本讨论的旧证据可复用，不能重复灌多轮浪费模型。
4. 用户确认的是页面所见当前最终分镜，独审通过后才进入报价；批准前媒体create=0。若检查提出真问题，记录并通过讨论做一次有依据的修订；不能假pass。

### Q02 报价和合理购买

在原主片当前报价依次有目的验证“暂不继续”（0create、可回来）、“调整方案”（如果确需更省且不损质量，只动相关节点、重新确认/报价，旧授权无效）、“同意并继续”。无需为了测按钮反复真正生成方案；调整方案完整负例由C15/原授权回归保护，真实未触发部分注明。

记录quote版本/digest、授权scope/上限、允许的Provider/model/镜头、执行前余额。先检查一次点击/重复刷新不会多买，禁止试着多点确认制造并发。

### Q03 素材、预检、声音、渲染

- 实际媒体请求ID、Provider状态、账单/receipt、素材SHA；复用关系必须真的复用，失败停节点而非说明卡。现有画面预检检查主体/动作/跨镜一致性，失败先记录具体责任，不能删标准过关。
- 实际TTS输出、最终演员/语速/停顿参数和有效配置一致。完整听音，中文自然度、断句、空白、旁白节奏/画面对齐；不只ffprobe音轨存在。
- 渲染完整成片，ffprobe核对尺寸、fps、总帧/时长，逐段播放覆盖头中尾；不得错误采样又称黑屏。关键动作/结尾兑现、画面风格、乱码/生成文字、说明卡、字幕与旁白都检查。技术有效不等于高质量。

### Q04 同片双真实视觉审片、局部返工与交付

两个不同实际视觉模型，读取同一成片SHA/同一证据集合；保留各自报告/身份、聚合结论。文字role-audit、同一模型两个session、旧片报告不能替代。

若拒绝，按实际finding定位责任与受影响镜头；若通过而需验证返工，使用明确标注“QA人工提出”的合理单镜修改，不伪造审片发现。一次有界局部返工：预填真实要求→必要上游讨论确认→新范围报价→未影响媒体SHA复用→新片/新双审。报价超剩余额度则停购买，写缺口，不扩大总授权。

最终定版后检查交付文件、下载、预览、素材库按视频组织、复盘显示真实状态。不执行外部发布。

### Q05 系列与图库路线修订

原系列run `run-62324471-a9bd-4e6d-9b90-5cfe049dc700`，晨光观察、暖灰、不固定人物、自然旁白、AI示意非实验。走正式恢复到分镜讨论；上一完整方案与图库失败历史在下一实际模型输入中可证，已修镜头不无故回图库。到合法分镜确认/报价即足够，不买第二条主片。变化后一次受控恢复仍失败则记录，不换题。

### Q06 热点正文到制作

主片模型阶段结束/队列空闲后，一次真实热点刷新。核对read/partial/title_only，选择一篇实际原文核对段落与候选事实边界。看候选收益/视觉表现/缺证提示是否清楚；合法创作标签不再被词面归零；不是强求固定数量。

采用一个真实可制作方向，走到导演讨论，检查冻结的来源/不确定项真的进入角色。不购买第二片。全被来源门拦截时区分合理缺证与新的误判，写明未完成正链，不重复刷新碰巧换到容易新闻。

### Q07 真实缺料负例与原能力回归

原负例 `run-6083f24f-bc98-4fb5-81f2-2aeb3954e285` 要求本人两次录像和计时却没提供且禁止模拟；原repair76应保留，不能因修图库而放行。以现有稿和门禁检查为主，不再生成一次相同负例。模型保存原已证明上游SHA不变，本轮检查未退化。

声音预设不换演员、unsupported设置不伪称生效；搜索声音同页落点；模板CRUD保留但不参与新制作；素材库/复盘/历史读取。只对自己新建的临时CRUD记录做删除并记录，不碰用户历史。

### Q08 恢复、统计和手机（可与无模型等待并行）

- 同一主片记录当前/历史request与模型attempt，截图数值对照durable。模型任务完成后正常刷新、离开回来、同workspace重启仍一致；不杀在途模型。
- 两窗口只做版本冲突的安全编辑/确认验证，旧窗被拒不覆盖新稿；不并发制造媒体购买。正在输入时轮询不抢焦点。
- generic retry终态详情/诊断无需F5出现；无“已停止”同时“正在生成”。缺源/正常讨论/unknown用不同解释。
- 390×844、较矮手机窗口（例如390×667）、桌面；短/长输入、切tab、滚到底部、撤销可用/禁用、中文IME、刷新草稿保留。发送/确认/撤销分别做hit-test，命中自己的按钮或子元素；禁止用真实发送仅为测坐标。浏览器不能真实模拟系统软键盘时注明模拟viewport证据与未验证限制，不声称实机通过。

## 四、有界测试与记录纪律

每个操作先说明验证什么，不无脑全按钮点一遍。明确合同错误不用再发一次证明；unknown仅观察原ID。有真实任务进展则按有界轮询等待，记录受理、排队、模型开始/结束、本地消费、用户可见时间，不把人工等待算慢。

一个阻断出现后先查原请求/相关日志，记录已证实原因与未确定部分，继续独立可测项。集中修复后，同根因同路径最多一次有条件变化依据的复验；再失败则停该链，不新建run/换模型/降低质量碰碰运气。产品安全（错误付费、绕确认、数据丢失）问题立即停止相关写入。

每个QA行写 PASS/FAIL/BLOCKED/NOT_VERIFIED/PARTIAL、run/stage、操作与预期/实际、requestId、实际模型/effort、耗时拆分、费用、截图/日志/成片SHA、根因证据和下一责任节点。记录无费用也写未走到还是合法免费。未知按未知，不把0账单自动解释为没调用。

先做这一整轮，按范围集中补修必要阻断及受影响回归后做一次复验；其它新问题统一报告，不开启无限优化循环。最终 `RESULT.md` 链接新QA报告/证据目录，并明确：是否真的有成片、是否听音、是否有同片双模型报告、是否验证局部复用返工、是否交付。只编译通过不得收口成“全部完成”。
