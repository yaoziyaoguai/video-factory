# 给普通 coding agent 的执行指令

你负责 VideoFactory R11 QA 后的集中修复、确定性回归与真实本地验证。直接开始执行，不要只回复计划，不需要方案窗口逐小项审计。

**本任务包含修复和测试两个连续阶段：集中修复 → 编译/类型检查/确定性回归 → 直接部署真实本地环境 → 使用 QA 技能完成浏览器 QA 和端到端测试 → 记录报告。编译通过不是结束，不得停在“等用户允许开始 QA”或提前 READY_FOR_REVIEW。** 真实验证有预算和重试边界；不能以必须跑通为由绕过确认、伪造结果或无限重试。

仓库：/Users/jinkun.wang/work_space/veidofactory
资料包：/Users/jinkun.wang/work_space/veidofactory/docs/codex-collaboration/r11-qa-repair-20260914
任务：VF-R11-QA-REPAIR-20260914

## 1. 开始

完整读取本目录 README、FINDINGS_AND_FIXES、VALIDATION_AND_QA 和 EVIDENCE_FINAL_UPDATE.json；BASELINE.json 用于机器核对文件摘要，两个旧 EVIDENCE 快照仅作时间线参考。补读最新 R11 QA 报告、RESULT 的最新 R11 记录、revision11/implementation/01_PRODUCT_AND_UX.md、02_TECHNICAL_DESIGN.md；按测试文件读取原 D11/Q11 验收和现有 QA/浏览器技能。上述相对路径均以资料包或 README 链接为准，不去找其它项目。

确认真实目录、HEAD、分支、暂存区、适用 AGENTS、运行时与文件摘要。预期 HEAD 是 b36ebac3189cf574b8e437cbe16eb9d0b228a177；历史 570fe6e 绝不是回退目标。当前有大量未提交产品修改与未跟踪证据，全部保留。BASELINE 是本次交接时内容，不是让你恢复的备份；若其它窗口有新成果，核对后在其上继续，不能覆盖。执行端独占本次产品修改，发现同时写同一文件时先解决真实冲突。

当前 TASK 仍是 R11，已有三阶段用户讨论确认、热点原文阅读、R10 修复等实质实现。只修本包 F01–F11 及其共同根因/覆盖缺口；其中 F03/F07 是共享模型故障，不能重复造两套修复。不重新实施 R11 或 A/B/C，不恢复固定模板，不新增监工 Agent、生产链、数据库或框架。旧 R10 探针/审计不是当前仍失败的证明，不能照旧报告重复修已完成能力。

## 2. 已授权的动作：完成实现后自行测试

用户已明确授权你完成本项目修复所需的全部正常本地实现和测试操作：

- 修改范围内产品代码、正式测试、必要文档；运行已有测试、类型检查、构建与 source/dist 组合验证。
- 启动、构建并在确认任务安全后正常重启本项目真实本地 Studio、OpenAI Broker、ZAI Broker；建立本轮独立持久化 QA workspace/socket/本机端口；纠正本项目启动配置。
- 读取和使用启动/诊断本项目服务必需的既有配置、登录凭据与密钥；秘密只进入所需本地进程，不回显、不写报告、不上传。不得扫描无关私人文件。
- 使用当前配置真实模型与强度，读已配置 Provider 的公开文档，定位实际 API/CLI 失败。已明确是外部请求必要组成的 QA 输入和素材可提交给对应已配置服务。
- 使用当前可用浏览器与 QA 技能，自行创建 QA 机会/系列/样片；代用户完成解释、备选、采用、撤销、阶段确认、模型与声音保存、暂停/继续、查询、刷新及恢复测试。
- 在以下总预算内自行点击“同意并继续/同意追加”，生成/获取媒体，自动配音、渲染、两个不同真实模型独立审片和有限局部返工；可以对本轮样片进行真实本地终审和本地交付，不做外部发布。

上述动作不用逐次问“是否允许测试/重启/使用密钥/确认这笔报价”。权限已经给出，不能把普通定位工作交回用户，也不能写成 WAITING_AUTHORIZATION 而停在构建通过。

费用延续 R11 这一轮共用的 `¥50.00` 测试止损，跨 run/session/agent/重启累计，包含媒体、TTS、其它真实按量调用与在途/未知最大暴露。不是每片 50 元，不是新增一轮 50 元，也不是产品全局预算。执行前核实当前账本；未知不能按 0 重试。订阅内文本/审片无新增现金则记录调用/时间即可。

继续遵守：不 commit/push、云端部署、外部发布、充值或新增订阅；不删除/迁移历史数据、不 reset/stash/clean，不清空 checkpoint/durable 解锁，不降模型/思考强度/输出容量/质量门槛。不升级工具或外发私有资料做 Oracle 审计。仅在超过本轮剩余额度、确需用户的新登录/验证码、破坏性操作或新产品/架构决定时报告具体证据和最小待决项。

## 3. 连续执行

1. 按 README 的批次顺序，为 F01–F11 建立失败序列证据。允许聚焦红绿，尽量用正式模块组合替换外部 transport，避免再用真实长任务复现已明确合同/状态错误。
2. 集中完成共同根因：Broker 安全诊断与 role-audit/topic-ideas 共享执行问题；GLM 同步结果不明的有界观察；预算合同；operation/review/UI 终态传播与同 run 草稿保留；热点正文真实网络接口；声音预设/能力说明、复盘和设置搜索。详细修正与允许模块见 FINDINGS；不把小项完成当作暂停点。
3. 检查原失败序列变绿，同时满足本包 V01–V15。补丁必须让正式工作流消费结果并合法推进，不能只验证按钮出现、FakePipeline 收到调用或摘要相等。已有全绿但绕过出错接缝的测试必须补强，尤其不能继续 mock 掉正文 requestPinned 后宣称网络修复。
4. 集中执行规定回归、类型检查、正式构建和 source/dist 验证；保存完整日志与退出码，自查实际 diff。新增用例必须进入正式测试入口。
5. 核实服务身份/新构建/在途任务后部署真实本地环境。利用主片最早确认前的真实调用完成 role-audit 就绪复验；失败有安全诊断且留证，不用修改状态/禁审绕过。
6. 进行一轮有界真实 QA：先记录，继续不受阻的独立用例；不边点击边改代码或频繁重启。同根因只在输入/环境/代码证据实质改变后允许一次受控复验；再次同因失败停止该链。
7. 收集后集中修复本任务内发现的同类遗漏，再运行受影响回归和一次必要真实复验。不得无限“改一点→换题→再跑”；若仍失败，交付失败原因与证据。

审计失败不能用只换成 GLM 而隐藏 OpenAI 缺陷；换模型验证是独立覆盖。accepted_unknown 只观察原请求，不新 ID/换模型重提。用户讨论不限产品终身次数，QA 重试上限不是产品限制；不要复活 R9 尚未实施的终身三次方案。

## 4. 记录与交付

过程统一追加 docs/codex-collaboration/RESULT.md 的“R11 QA 集中修复”段。它就是本轮工作日志；不另起 JOURNAL，不覆盖旧资料包中的历史 JOURNAL。QA 新报告用 docs/qa/videofactory-real-local-qa-<实际日期>-r11-repair.md，与旧报告区别开；若该报告已存在则加唯一序号，不覆盖证据。不改 TASK/REVIEW/本包验收迁就实现。

记录真实基线差异、F01–F11逐项根因证据和修改、V01–V15 和 Q11-01～Q11-12逐项结果、完整命令/退出码/失败/skip、实际模型和强度、调用/时间、报价与现金/未知台账、截图/原请求/成片/SHA/两份独审/返工证据。上下文切换前写清工作点、未结请求、剩余额度与下一步；新会话从此继续，不重新开预算或重复前序真实任务。

分别报告 CODE_VALIDATED、QA_PASSED 或 QA_DONE_WITH_CONCERNS、产品验收状态。全部范围实现并结束有界验证后 READY_FOR_REVIEW，停止产品修改；不能自行写 ACCEPTED。若关键复验仍失败，如实写 CHANGES_REQUIRED/BLOCKED 及未定位项，不用测试总数或健康分宣称全链路完成。
