# R11 Claude Code 接管现场

记录时间：2026-09-14 20:24（Asia/Shanghai）。用户要求停止当前执行，改由 Claude Code 接管。**产品未通过验收；最后修改未完成验证，不能直接据此重启或重试。** 本文件补充并纠正此前资料的实时状态，不替代产品约束。

## 1. 目标与边界

先完成同一主片：素材审查 → 配音 → 渲染 → 同片双真实视觉模型审片 → 一次合理局部返工与素材复用。之后集中处理旁支与性能。不要重写框架，不换题/新建run刷成功，不题材特判，不以说明卡兜底，不降低审片门槛或模型/思考强度。

沿用R11累计¥50测试授权，不重开预算；合理报价可自主确认。当前已知累计媒体¥13.50（配置费率计价，非冒称Provider正式核账），没有新增媒体在途/未知任务。后续每次购买前核对本轮全部run的实际现金、在途/未知与未付授权余额，父子scope/复用去重。不得commit、push、云部署、删除用户历史或清checkpoint。允许修复、构建、真实本地服务部署和有界QA。

先读本文件，然后读取：

- `docs/qa/videofactory-real-local-qa-20260914-r11-closure.md` 全文。
- `docs/codex-collaboration/RESULT.md` 最新“R11 主线续测”及后续记录；更早的状态不能当当前状态。
- `docs/codex-collaboration/r11-final-closure-20260914/` 的 MASTER_PROMPT、AUDIT、IMPLEMENTATION、VALIDATION、REAL_QA。K1–K6多项已经改过，不重新按旧清单全做；以当前源码和证据为准。
- 在涉及产品决策时查 `docs/codex-collaboration/revision11/implementation/01_PRODUCT_AND_UX.md`、`02_TECHNICAL_DESIGN.md`、`05_ACCEPTANCE.md`。

## 2. 仓库和运行现场

- 仓库：`/Users/jinkun.wang/work_space/veidofactory`。
- HEAD：`b36ebac3189cf574b8e437cbe16eb9d0b228a177`。
- 分支：`codex/final-dual-review-cloud-acceptance`。
- 大量dirty是已有成果；逐项看diff，禁止覆盖/回退整个文件。没有commit或push。
- 主run：`run-a7c42cc4-98de-4354-9dc9-c268b68b1446`，revision21，failed。
- run：`workspace/qa-r11-repair-20260914/runs/<runId>/run.json`。
- brief、creative-planning、assets均succeeded；asset-source-review failed；配音/渲染/最终双审/局部成片返工均未到达。
- Studio：`http://127.0.0.1:4319`，上次核实PID73442；OpenAI73439，ZAI73441。PID只导航，操作前核对实时进程、cwd、健康及在途。
- runtime：`.local/runtime/qa-r11-repair-20260914`，已有`start.mjs`，使用正式dist启动各服务；socket为`{openai,zai}/worker.sock`，日志为`{openai,zai,studio}.log`。Studio健康路径是`/api/health`，Broker为`/health`。
- 仍运行19:31的旧dist；**本次最后源码改动尚未构建或部署。** 不动旧4317服务或不明旧GLM任务。
- Node使用已工作的`/opt/homebrew/bin/node`（v26.8.1）；不要为测试换Node或重建ABI。
- 本地登录配置`.local/runtime/qa-auth-20260912.env`，变量`VIDEO_FACTORY_AUTH_USERNAME`、`QA_LOCAL_PASSWORD`。仅正常登录用，不回显/复制凭据。正式API写入需`x-video-factory-request: studio`；此前漏头403不是鉴权缺陷。
- `/tmp/vf-main-status.mjs`可只读查看脱敏状态；临时日志/脚本可能被系统清理，不能当永久证据。

## 3. 已购买并保留的素材

目录：主run的`nodes/assets/attempt-3/`。

- 镜头1：`scene_01_hailuo-video-v1.mp4`，task `441687379644690`，¥5.50，SHA `7d414749a6207fd3bb1e2fb50b75cf9e4a7f36f1817f05e1a9fb7c222e491543`，11.542秒/277帧/24fps/768×1344；2–4复用该母片。抽样有真实生成画面，不是说明卡；尚非完整质量验收。
- 镜头5：`scene_05_hailuo-video-v1.mp4`，task `441663804842287`，¥4，SHA `a5955aa425df77222aecd6b9d6ac9dee75f0ec7f62a34699c60bae7c42ed029f`，本次复用未重买。
- 同轮父run `run-25b4c195-a50c-4344-98fd-197d1bf58438` 另有¥4，所以累计¥13.50，不是仅本片¥9.50。
- 原¥9.50授权已幂等续接成功，未接受错误的¥11追加。终止操作的未提交prepared释放已修，submitted/unknown仍占用。不要回退此保护。

## 4. 当前主线阻断：必须沿代码查，不得笼统记录“语义不符”

请求：`agent-86fa82e02a8327b7671a98082a9d6f843bf35e3a9f502e09ba85f5313019329e`。

durable：`.local/runtime/qa-r11-repair-20260914/zai/tasks/.video-factory/codex-idempotency/zai/671c4cc65d32e7c002634393f5769e2146f2637dffc928240b3e90d2ae4df2f3.json`。

checkpoint：主run的`nodes/asset-source-review/agent-loop-checkpoints/0542216e5118067bfc8cd685005fdd99a280ee4fce248967a9f44132c6ec7f8e.json`。

已核实：

- 11:57:55.008 UTC受理，12:14:24.159 UTC completed failure；HTTP422/invalid_output/task_semantics。
- `providerWaitMs=989138`，queueWaitMs0，modelAttemptCount1，structuredRepairCount0，finishReason=stop。
- 模型glm-5.3-flash/max；promptTokens24036，completionTokens38311，其中reasoningTokens36514。非排队、非网络超时、非多次重试；不能据此认定思考慢的完整根因已查清。
- 15张帧，reviewContext约15367字符，payload约834671字符（含base64）。不要在日志/报告输出图片base64或完整prompt。
- ZAI executor已通过JSON和schema，在`outputSemanticValidationErrorFor`拒绝。`zai-code-plan-executor.ts`抛出错误时原message包含具体规则；`task-definitions.ts`旧诊断只映射director，不映射visual；`broker-server.ts`的`publicExecutorMessage`脱敏为generic，durable仅留下task_semantics。
- 原始响应和具体规则未在已查durable/checkpoint保留，ZAI响应为内存处理；**不能宣称已经知道本次具体违反哪条。** 安全诊断丢失和prompt漏写部分约束是确认的缺陷，但不等于已证明原片报告为何失败。
- 视觉校验共有七类：低分仍approve、低confidence仍approve、未消解finding仍approve、时间区间不包含timecode、failed没有有效返工组合、not_observed不是info/inspect_existing_media、satisfied/not_applicable却请求返工。
- 原directive有大部分业务要求，但未明确failed不得info、not_observed必须info，以及start<=timecode<=end完整关系。最后patch把这些明确补入输出规则，**没有放宽validator，没有添加自动通过或新的语义修正重试。**

## 5. 最后改动：未完成，接手要先检查

本次最后产品diff仅新增到以下已有dirty文件中，不能覆盖其原修改：

1. `apps/codex-broker/src/task-definitions.ts`：visual prompt v15→v16，补四条跨字段规则；为全部七类visual语义拒绝添加白名单reasonCode/fieldPath。没有改validator判定或评分。
2. `packages/production-pipeline/src/codex-chat.ts`：visual合同digest同步为`81f76a8f2d026ef273503333ad4de0f60346da94b54c077219c244e31097f997`。
3. `apps/codex-broker/test/task-definitions.test.ts`：版本和输出规则断言。
4. `apps/codex-broker/test/zai-fallback-integration.test.ts`：新增七种受控输出经真实ZAI executor→Unix socket Broker→client→同ID重放的集成测试；只替换外部fetch，不接正式环境。

新增测试先红：预期visual_approval_score，实际task_semantics，证实诊断丢失。日志`/tmp/vf-visual-contract-red.log`。

改后聚焦命令：

```sh
node --test --test-concurrency=1 --import tsx apps/codex-broker/test/task-definitions.test.ts apps/codex-broker/test/zai-fallback-integration.test.ts apps/codex-broker/test/zai-code-plan-executor.test.ts
```

**实际66 pass/1 fail，exit1**（日志`/tmp/vf-visual-contract-green.log`，文件名green不代表通过）。新增集成用例得到的error.statusCode是undefined，预期422。尚未展开真实error定位，可能涉及组合接线/合同边界；不能自行归类为stale、环境或fixture问题。先保留脱敏error全部字段查明再改，不能删测试/弱化断言过关。没有对本次patch做build/typecheck/全量，不得声称修复验证完毕。

此前费用修复后新增的`production-authorization.test.ts`三个宿主/持久化集成用例已3/3，与费用/素材/Studio授权合跑144/144，exit0。该改动也保留。

## 6. 其它验证与待查，不混淆新旧结果

- 最后这组prompt/诊断patch之前：TS全量828 pass/0 fail/1既有真实E2E开关skip，exit0；pipeline typecheck exit0；下游试片/审片/预检42/42。
- 同时Broker全量211 pass/2 fail，exit1：`codex-bridge-recovery.cross.test.ts`前两个测试固定20/30ms后断言executor已启动，actual0/expected1；原样单跑2/2 exit0。尚未修。必须核对生命周期事实，改为真实开始事件同步，不能仅增加sleep或把失败隐藏掉。
- 日志`/tmp/vf-main-full-ts.log`、`/tmp/vf-main-broker-regression.log`、`/tmp/vf-main-resume-regression-final.log`、`/tmp/vf-main-downstream-regression.log`。
- 20:24 `git diff --check`未输出错误；这不是编译/产品验收。
- UI当前同时写“可直接重试”与“不要反复重试”；列为诚实恢复文案待修。
- UI历史待核费与本轮无在途事实不一致，待核对投影，不能删除历史账本解锁。
- PERF-R11-01保持OPEN：慢即使成功也要追查；保持模型/effort/必要证据，查输入重复、推理负担、时间拆分和重复审查职责。不要把降低token或强度当性能修复。
- 实际声音配置是macOS Tingting、185、pauseScale1、natural，不是MiniMax；后续真实音轨不得冒称验证了MiniMax API。
- 最终审片组合是GLM+Codex两个实际视觉模型；GLM视觉报告再由GLM做报告审核不等于同片双真实模型审片。

## 7. 接管顺序和停止纪律

1. 先检查最新git diff、上述新测试实际error及七类规则全路径。判断现有小patch是否充分，不把诊断增强当根因全修复。若有新修正，先用确定性测试复现，保持安全合同，不调用真实模型找语法问题。
2. 集中跑受影响回归、类型检查、正式构建和source/dist合同组合；未通过不启动真实复验。构建串行，不能让两任务清写同一dist。
3. 核对本轮服务空闲后，只正常重启这三个已确认属于本轮的服务，保留workspace/媒体/checkpoint。确认实际模型/合同就绪；不发无目的探针消耗订阅。
4. 条件变化后通过正式UI恢复原run一次，仅重做受影响审查，不重新购买已物化媒体，不伪改失败结果/分数。若同根因再次失败，记录具体request、规则、责任和证据，不连续重试、不换run刷通；继续独立可测项。
5. 主线走配音、完整渲染、同片双审与局部返工。按当前本地QA技能真实点击、听音、播放、截图、移动布局、刷新/重启验证；API测试与点击证据分开标注。费用按累计50元，后续超授权交用户决定。
6. 过程统一追加RESULT及同一closure QA报告，明确PASS/FAIL/BLOCKED/NOT_VERIFIED、现金与未知暴露。主线后集中处理其余旁支和慢因，不无限优化。无成片/双真实审片/局部返工证据即PRODUCT_NOT_ACCEPTED。

移交时：没有新增真实请求或购买，没有重启服务；原素材审查已终态失败，当前没有在等待它返回。前一执行窗口已停止修改，接管者独立核对环境后继续。
