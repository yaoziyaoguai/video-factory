=== R11 收尾轮 · step-r26 查明：返工被拦在镜头 4 上，根因是两个叠加缺陷 ===
记录时间：2026-09-15
run: run-6ad4e264-8385-45ae-84a0-52d5ef5cc925（返工，sourceRunId=run-f8e2635f-4baa-4a4d-9a2b-a6e169ee5c08 rev 21，affectedScenePositions=[6,7]）
付费增量：¥0.00（工作区累计恒为 ¥17.00）

------------------------------------------------------------------------
【先更正我自己的两个错误结论（都已用证据推翻，不删除原始读数）】
------------------------------------------------------------------------
错判 1（已推翻）："重规划改写了镜头 4 的检索词"
  反证：返工草稿 attempt-1/attempt-2 与源 run attempt-7 的镜头 4 逐字段比对——
        deliveryType / preferredProviderId / query / subject / estimatedCostCny /
        sourceInSeconds / rationale 全部相同。query 均为
        "vertical close up unbranded hot drink steam backlit warm city background"，
        rationale 均含"语义排序采用 pexels/7577435"。
  另证：我发出 request_changes 前后（attempt-1 13:17:04 / attempt-2 13:21:43）
        场景 4 逐字节相同 —— 该指令并未改变场景 4。

错判 2（已推翻）："素材真的找不到了"
  反证：见下，候选集完全相同，只有模型打的分变了。

------------------------------------------------------------------------
【缺陷一：跨 run 的证据携带只做了一半（导致重新打分，分数飘移否掉原方案）】
------------------------------------------------------------------------
系统自述的不变式（packages/production-pipeline/src/creative-planning.ts:385 附近）：
  "候选搜索指纹只覆盖仍依赖图库的镜头（stock 交付）的完整检索身份：……重修后指纹未变
   说明候选条件未变，不得重搜重排；改用生成路线或改写检索词都必须重新搜索。"

但跨 run 播种（packages/production-pipeline/src/production-pipeline.ts:7208 起，
seedJointPlanningThread 调用点约 :7234）只覆盖 treatment 与 script 阶段；
candidates 与 rank 不在播种范围。返工是"新 run"，state 里没有前任候选/排序证据，
于是即便检索身份一字未变，仍从头重搜重排。

镜头 4 铁证（同一候选集，只有 LLM 分数不同；门槛 = AUTOMATIC_CANDIDATE_SEMANTIC_MINIMUM = 40，
见 creative-planning.ts:117）：

  源 run  （nodes/creative-planning/attempt-8/candidate_ranking.json）
    7577435:72  7236863:65  13736645:38  7144284:34  6278949:29   → 最高 72 ≥ 40 ✅
  返工 run（nodes/creative-planning/agent-loop-checkpoints/e719ebc6….json）
    7144284:38  13736645:32  7577435:25  7236863:22  6278949:18  6206442:5 → 最高 38 < 40 ❌

  候选 assetId 集合相同；同一个 7577435 从 72 掉到 25，7236863 从 65 掉到 22。

后果链：重搜重排 → 绝对分飘过门槛 → defaultAvailabilityReviewer 报 director 级问题
（creative-planning.ts:481）→ 同一 digest 重复出现判为 stalled → 升级为 target:"source"
（creative-planning.ts:1192/1223）→ 停在下述阻断项，要求操作员"改方案或停止制作"：

  [{ "target":"source", "scenePositions":[4],
     "reason":"图库候选不足：该镜头没有达到自动采用语义阈值的候选",
     "requiredChange":"调整检索词，改用已启用的生成或复用路线，或人工补充合适素材；也可以停止本次制作。" }]

对照：源 run 的 creative-planning = succeeded 且 blockingIssues 为空（同一份方案）。

------------------------------------------------------------------------
【缺陷二：跨 run 复用只覆盖付费素材，不覆盖图库素材（导致无关镜头被重新取材）】
------------------------------------------------------------------------
generative-asset-worker.ts:1629 effectiveSourcePaidAssetItems 读源 run 的付费素材账本
（nodes/assets/.generation-operations/<sha256(operationId)>.json，取 state==="materialized"
且文件通过 SHA-256/size 校验的条目）。

源 run 该账本实际内容：镜头 1(seedance,¥5)、镜头 2(seedance,¥5)、镜头 3(hailuo,¥2)、
镜头 6(seedance,¥5)。**镜头 4、5 是 pexels 图库素材，不在此账本内。**

对照：镜头 4、5 的素材确实存在于源 run 磁盘：
  nodes/assets/attempt-4/assets/job-1/scene_04_pexels_7236863.mp4
  nodes/assets/attempt-4/assets/job-1/scene_05_pexels_6666665.mp4
（同一 attempt 的 generation_jobs.json 只记付费镜头 3、6；图库镜头走 assets/job-1/asset_plan.json）

结论：图库镜头不在跨 run 携带集合内 → 返工时被重新路由回图库重新取 → 触发缺陷一的重搜重排。

------------------------------------------------------------------------
【为什么这仍是内容无关的通用缺陷】
------------------------------------------------------------------------
只要走"返工"这条路，任何题材的图库镜头都会被重新检索、重新打分，都可能被
LLM 打分的随机漂移否掉一份原本成立的方案。与主体、题材、镜头内容无关。

------------------------------------------------------------------------
【已排除的可能（另有证据）】
------------------------------------------------------------------------
· 不是"重买已成功素材"：本条返工至今付费增量 ¥0.00，提交载荷 affectedScenePositions=[6,7]。
· 不是 checkpoint 缺失：判据用 review.json / nodeRuns 而非 checkpoint 文件是否存在。
· 不是我的 request_changes 没被执行：revision 0→1、attempt-2 生成于 13:21:43（指令之后）。

【未做的动作】
· 未点「重新检查已有试片」（同批帧重跑审片 = 刷成功，禁止）。
· 未点「修改后再审 / 终止制作」。
· 未在任何一步降低门槛、放宽阈值或按题材特判。
