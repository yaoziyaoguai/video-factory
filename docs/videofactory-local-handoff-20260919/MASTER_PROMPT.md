# VideoFactory 本地实施与验证总入口

日期：2026-09-19。状态：资料包已准备，产品实施未开始。

你是执行端 coding agent。请接管真实仓库：
`/Users/jinkun.wang/work_space/veidofactory`

目标：在当前架构上完成剩余治理、恢复和交互工作，使用户能低门槛地与制作角色合作制作高质量视频；模型提供意见，用户掌握推进权。不是重建项目，不是按某个题材补丁式修复，也不能承诺“爆款”结果。

## 一、完整读取与权威顺序

本目录绝对路径：
`/Users/jinkun.wang/work_space/veidofactory/docs/videofactory-local-handoff-20260919`

按顺序完整读取：

1. `BASELINE.md`：本次核实的代码基线和证据限制。
2. `01_STATUS_AND_DECISIONS.md`：当前事实、已定产品规则、交互流程。
3. `02_IMPLEMENTATION.md`：顺序实施批次、改动边界、实现路径。
4. `03_ACCEPTANCE.md`：失败测试、跨层验收和旧编号映射。
5. `04_LOCAL_QA.md`：真实本地验证、费用权限、有界测试。
6. `05_BACKLOG.md`：明确不在本轮实现的图库/视频库扩充。
7. `RESULT.md`：本轮唯一实施结果与续接入口。

最新用户决定 > 本包新合同 > 当前源码事实 > 旧材料未冲突部分。本包不是已实现证明。开始先核对 HEAD、git status、相关源码和测试；行号只是导航。HEAD 不同不要求回退，核对变更后更新基线。不要执行旧 HANDOFF 中的 checkout/恢复文件指令。

历史材料只用于查需求或原因，不必每轮从头读几千行：
- `docs/VideoFactory-五维审计与单会话实施计划-2026-09-18.md`
你是个傻逼吗？你正常的测不就行了吗？正常登录不就行了吗？搞个毛线啊在这里，就按原来正常的 QA 就行了。我为什么要给你登这个密码？烦死了！正常的登录，不要给我导入到你自己里面了.local/audit-2026-09-18/VideoFactory-全项目审计与CodingAgent实施手册-2026-09-18.md`
- `.local/audit-2026-09-18/evidence/B3-gpt-retirement/PLAN.md`
- `.local/dogfood/FINDINGS.md`

## 二、执行方式

连续按 P0 → P1 → P2 → P3 → P4 执行；P0 只是核对，不重做已完成改造。每批先建立关键失败证据，再集中修同一合同的代码，跑聚焦回归。全部完成后统一全量验证和本地 QA，不逐小步启动外部审计，也不做一点就交还用户。

发现现有实现已经满足某项，只补缺失证据，不制造重构。新增测试必须进入现有 runner，不能留下孤儿测试。遇环境问题先定位运行时/ABI/构建版本，不篡改业务断言。

不要在用户未授权的情况下自主启动其他 coding agent。本轮使用当前执行端已配置模型，不擅自降模型、降思考强度或删质量要求。会话压缩后先读 RESULT 的“当前接续”，不重新推导已定产品原则。

## 三、不可突破的边界

- 仅本地；不 commit、不 push、不上传 GitHub、不做云部署。
- 不调用已退役 GPT/Codex/ChatGPT 生产路径；DeepSeek 单模型审片。保留共享 Broker 和历史事实，不按 codex 字符串删除代码。
- 保留用户已有代码、文档、历史 run、媒体、费用和回执；不 reset/clean/stash，不批量删除或迁移数据。
- 不新增数据库、缓存服务、工作流框架、授权平台或第二套生产链。
- 不扩大模板约束，不用模板/系统建议覆盖用户明确要求。
- 不降低模型质量来缩短耗时，不回滚已有 32MiB 流上限修复；区分排队、首输出、生成、轮询、审查、人工等待。
- 不引入题材、镜头位置、runId 或特定响应文本的通关特判。
- 不让模型低分直接裁决作品失败；但无完整回执、缺素材、非法请求、unknown 扣费或无付款授权不能“承担后跳过”。
- 用户暂时沿用旧 DeepSeek key。不得读取并输出密钥、擅自轮换、创建订阅或把凭据写进资料。
- 当前没有新金额授权。旧 ¥50 不延续。真实新增收费任务（包括文本 API）必须先明确授权；其它免费/离线工作继续。
- 最终作品是否接受，由用户决定，执行端不能以测试通过代签。

## 四、停下来问用户的条件

仅在新产品取舍、明显超出本包架构范围、用户数据破坏性操作、凭据处置、新付费授权或不可替代环境阻塞时询问，给最小问题和推荐。若只有真实收费验证被阻断，先做完其它实现、确定性测试与免费浏览器测试。

不需要再问：是否恢复双审、是否轮换 key 才能写代码、是否上云、是否换工作流框架、是否允许模型替用户自动否决——这些已经决定。

## 五、交付

统一更新本目录 RESULT.md：实际修改、失败→通过证据、命令/cwd/退出码/日志、验收 ID、实际请求和费用、未验证项、下一接续。区分 implemented、tested、verified_live、blocked、not_run、deferred。

完成编译和确定性回归后自动进入权限允许的本地 QA；需要新付费时再问。报告主线是否真的到达成片、本地下载包和用户验收停点，不只交测试数量。结束时给出 diff 和仍未完成事项，保持不提交。
